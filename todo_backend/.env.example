# Example environment variables for the Todo Backend (FastAPI + SQLite)
# Copy this file to ".env" and adjust values as needed.

# SQLAlchemy-style database URL.
# By default, the app uses sqlite:///./todo.db if this is not set.
# To connect to the shared SQLite database container, point this to the absolute path of the SQLite file.
# Example: sqlite:////absolute/path/to/myapp.db
DATABASE_URL=
